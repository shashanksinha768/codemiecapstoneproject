{
  "name": "travel-accommodation-search",
  "version": "1.0.0",
  "description": "Travel accommodation search with advanced filters - Capstone Project",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "start": "cd backend && node src/server.js",
    "build": "npm run install:backend"
  }
}
